__pycache__/
*.pyc
.pytest_cache/

# Large GenHuman source rigs at repo root — kept on disk, out of git (use git-LFS if ever needed)
/*.ma

# Local machine-specific Claude settings
.claude/settings.local.json

# User's saved library locations (per-machine; path.txt.example is the tracked template)
scripts/path.txt

# Packaged artist deliverable bundles (build artifacts, not source)
/Outfitter_*.zip

# Loose source icon drops at repo root — the canonical copy lives in the package
/shirt*.png

# WSL "downloaded from the internet" alternate-stream junk
*:Zone.Identifier
